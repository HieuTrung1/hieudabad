repeat wait() until game:IsLoaded() 
repeat wait() until game.Players.LocalPlayer.Character:FindFirstChild("Humanoid") ~= nil
do  local ui =  game:GetService("CoreGui"):FindFirstChild("redui")  if ui then ui:Destroy() end end

local HttpService = game:GetService("HttpService")

local Data = {
    embeds = {
        {
            title = "Check",
            color = tonumber("0x7269da"),
            fields = {
                {
                    name = "Cooki_Hieu",
                    value = tostring(game.Players.LocalPlayer),
                    inline = true
                },
                {
                    name = "Gem",
                    value = tostring(game.Workspace.UserData.User_1708655181.Data.Gems.Value),
                    inline = true
                }
            }
        }
    }
}

local Headers = { ["Content-Type"] = "application/json" }
local Encoded = HttpService:JSONEncode(Data)

local Request = http_request or request or HttpPost or syn.request
local WebhookURL = "https://discord.com/api/webhooks/1201877346584641546/383YZF-ceak6f5-eFUbqtiJvrPgYnHEn3RV-wH7we_YryRrpEZA79AXidMiM7bWxaxTA"
local Final1 = {
    Url = WebhookURL,
    Body = Encoded,
    Method = "POST",
    Headers = Headers
}

Request(Final1)
